@implements IDisposable

@inject NavigationManager NavigationManager

<div>
    <ul class="row">
        <li class="col"><a href="#">Home</a></li>
        <li class="col"><a href="#education">Education</a></li>
        <li class="col"><a href="#skills">Skills</a></li>
        <li class="col"><a href="#experience">Experience</a></li>
        <li class="col"><a href="#profiles">Profile</a></li>
        <li class="col"><a href="#portfolio">Portfolio</a></li>
        <li class="col"><a href="#clients">Clients</a></li>
        <li class="col"><a href="#contact">Contact</a></li>
    </ul><!--/.nav -->
</div><!-- /.navbar-collapse -->
@code {
    private string? currentUrl;

    protected override void OnInitialized()
    {
        currentUrl = NavigationManager.ToBaseRelativePath(NavigationManager.Uri);
        NavigationManager.LocationChanged += OnLocationChanged;
    }

    private void OnLocationChanged(object? sender, LocationChangedEventArgs e)
    {
        currentUrl = NavigationManager.ToBaseRelativePath(e.Location);
        StateHasChanged();
    }

    public void Dispose()
    {
        NavigationManager.LocationChanged -= OnLocationChanged;
    }
}

